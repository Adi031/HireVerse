spring.application.name=hireverseapi

spring.profiles.active=dev

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/job_portal_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Predator@3107

# Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Note: dialect auto-detected by Hibernate 6 from JDBC URL — no need to set manually
spring.jpa.open-in-view=false

# JWT Secret Configuration — PLACEHOLDER ONLY. Set real value in application-dev.properties (gitignored)
jwt.secret=YOUR_256_BIT_SECRET_KEY_HERE_REPLACE_IN_DEV_PROPERTIES
jwt.expiration=86400000

# Server Port
server.port=8080

